parameters:
    cl_slack.payload.transport.class: CL\Bundle\SlackBundle\Slack\Webhook\Transport

services:
    cl_slack.payload.transport:
        class: %cl_slack.payload.transport.class%
        arguments: [%cl_slack.webhook_url%]
